# Build outputs — regenerated by `pnpm build` / `pnpm cap:build`
dist/

# Android build artifacts (not the project itself — android/ is committed)
android/app/build/
android/build/
android/.gradle/
android/local.properties

# version.json is emitted by the Vite build into dist/public/ (not public/)
# No entry needed — it is never written to public/ after the vite-plugin change.
